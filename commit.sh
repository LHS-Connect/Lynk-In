# the python script to commit the changes to git, but for linux users :)
# ik its a bit redundant but it works, and its nicer to just type "./commit.sh" instead of "python3 commit.py" :)

python3 commit.py