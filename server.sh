# the python script to commit the changes to git, but for linux users :)
# ik its a bit redundant but it works, and its nicer to just type "./server.sh" instead of "python3 server-start.py"

python3 server-start.py